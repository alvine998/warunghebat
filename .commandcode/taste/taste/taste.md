# Taste
- Writes short, lowercase, code-switched (English + Indonesian) prompts with minimal punctuation, and expects the agent to explore the codebase and infer the missing details rather than ask for a full spec. Confidence: 0.6
- When asked to choose between designs, picks the agent's recommended option, preferring the pragmatic, dependency-light path (e.g. manual bank transfer + proof upload over adding a payment-gateway dependency). Confidence: 0.55
- Prefers a feature delivered end-to-end over a partial/staged slice (chose the whole cart → checkout → payment verification → escrow wallet → withdrawal → payout chain). Confidence: 0.55
- Maintains an `AGENTS.md` in the project with explicit constraints and expects it to be honored (e.g. do not create new base folders such as `app/Support` without approval — keep logic in existing locations like controllers). Confidence: 0.5
- For data visualization, wants a real charting library rather than hand-rolled CSS/div bars (explicitly asked for ApexCharts). Confidence: 0.5
- Wants user-facing "open in maps" buttons to link to Google Maps (`https://www.google.com/maps?q={lat},{lng}`) rather than OpenStreetMap, and expects the change applied consistently everywhere it appears (all maps/views, not just the one shown). Confidence: 0.55
- Product/store cards should navigate to store detail when tapped, with the redundant "Lihat →" hint removed, and any add-to-cart ("+ Keranjang") control placed as a separate button (only rendered for open stores with stock, never nested inside the link). Confidence: 0.55
- Established project conventions to match: money stored as integer rupiah and displayed via `Rp {{ number_format($n, 0, ',', '.') }}`; inline `$request->validate([...], [Indonesian messages])` instead of FormRequests; business/lifecycle logic on models; PHPUnit feature tests with `RefreshDatabase` + factories; Pint for PHP formatting; `npm run build` after Blade/Tailwind changes; all user-facing copy (labels, validation, flash messages) in Indonesian. Confidence: 0.6
mRequests; business/lifecycle logic on models; PHPUnit feature tests with `RefreshDatabase` + factories; Pint for PHP formatting; `npm run build` after Blade/Tailwind changes; all user-facing copy (labels, validation, flash messages) in Indonesian. Confidence: 0.6
